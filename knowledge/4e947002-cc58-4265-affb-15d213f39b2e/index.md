# STRATO

Partition key `4e947002-cc58-4265-affb-15d213f39b2e` — pass this as `brand` to the Hub's API, and read only this directory.

452 atoms in this directory, 114 of them global on all four axes. Retired and draft
atoms are not here: only what a person published reaches a target. Neither is a published atom
that left an axis undeclared — those are withheld and counted under "Attribution backlog" below,
if there are any.

| Dimension | Atoms | Categories |
|---|---|---|
| [`brand identity`](brand-identity.md) | 12 | `brand_context` 8, `personality` 1, `misc` 3 |
| [`writing & tone`](writing-tone.md) | 13 | `voice_register` 6, `web_readability` 5, `audience_framing` 2 |
| [`product`](product.md) | 310 | `description` 39, `value_proposition` 37, `differentiators` 52, `proof_points` 133, `claim_constraints` 47, `misc` 2 |
| [`image guidelines`](image-guidelines.md) | 31 | `photographic_style` 3, `composition_and_framing` 6, `subject_and_diversity` 8, `color_direction` 4, `visual_tokens` 7, `misc` 3 |
| [`design guidelines`](design-guidelines.md) | 60 | `color_usage` 25, `typography_scale` 12, `components` 10, `logo_and_brand_elements` 5, `layout_and_responsiveness` 3, `accessibility` 4, `misc` 1 |
| [`target group`](target-group.md) | 26 | `segment_definition` 5, `needs_and_pains` 11, `decision_drivers` 7, `addressing_the_segment` 3 |

The counts are what is PUBLISHED, not what a prompt gets. A prompt gets at most 12 per
dimension-category after your scope has filtered these. Global atoms get NO separate slice: an
atom tagged `*` competes in that same per-dimension-category cap, ranked together with the
specifically-tagged ones — see `../../skills/assemble/SKILL.md`.

